For usage in UNIX/Linux with Bash and Perl.

In order to run the bash scripts, use $ chmod +x to make it executable. Then, run the file with $ ./<filename>

In order to run the Perl Scripts, just use $ perl <filename>.pl